<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%- messages() %>

<%- content %>

<div class="mgt-container">
  <div>
    <h2>Manage Classifications</h2>
    <p>Select a classification to update:</p>
    <div class="mgt-inv">
      <table id="classificationDisplay"></table>
    </div>
  </div>
  <div>
    <h2>Manage Inventory</h2>
    <p>Select a classification from the list:</p>
    <div class="mgt-inv">
      <%- classificationSelect %>
      <table id="inventoryDisplay"></table>
      <noscript>JavaScript must be enabled to use this page.</noscript>
    </div>
  </div>
</div>

<script src="../../js/inventory.js"></script>